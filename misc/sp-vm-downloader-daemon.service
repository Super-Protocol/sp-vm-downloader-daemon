[Unit]
Description=Daemon to automatically download SuperProtocol VM images
After=network.target

[Service]
Type=simple
Restart=always
RestartSec=1
WorkingDirectory=/usr/bin/sp-vm-downloader-daemon/app
ExecStart=/usr/bin/sp-vm-downloader-daemon/app/start.sh

[Install]
WantedBy=multi-user.target
